<div id="btnWrapper">
    <div>
        <span></span>
    </div>
</div>
<header>
    <nav>
        <ul>
            <li><a href="#inicio">Inicio</a></li>
            <li><a href="#sobre-mi">Sobre Mi</a></li>
            <li><a href="#proyectos">Proyectos</a></li>
            <li><a href="#contacto">Contacto</a></li>
        </ul>
    </nav>
</header>

<style lang="scss">
    #btnWrapper {
        position: fixed;
        right: 0;
        top: 20px;
        width: 70px;
        height: 38px;
        border-radius: 50px 0px 0px 50px;
        background-color: #7F7DF3;
        padding: 4px 0 4px 8px;
        z-index: 1010;

        div {
            height: 100%;
            position: relative;
            cursor: pointer;

            &::before {
                content: '';
                position: absolute;
                top: 5.5px;
                left: 3px;
                width: 22px;
                height: 3px;
                background-color: #EEE;
                border-radius: 10px;
                transition: all .3s;
            }
            span {
                position: absolute;
                top: 14px;
                left: 3px;
                width: 22px;
                height: 3px;
                background-color: #EEE;
                border-radius: 10px;
                transition: all .3s;
            }
            &::after {
                content: '';
                position: absolute;
                bottom: 5.5px;
                left: 3px;
                width: 22px;
                height: 3px;
                background-color: #EEE;
                border-radius: 10px;
                transition: all .3s;
            }

            &.close {
                &::before {
                    top: 50%;
                    transform: translateY(-50%) rotate(-45deg);
                    transition: all .3s;
                }
                span {
                    opacity: 0;
                    transition: all .3s;
                }
                &::after {
                    top: 50%;
                    transform: translateY(-50%) rotate(45deg);
                    transition: all .3s;
                }
            }
        }

        @media screen and (min-width: 481px){
            & {
                display: none;
            }
        }
    }

    header {
        position: fixed;
        left: 100vw;
        width: 100%;
        height: 100vh;
        background-color: #7F7DF3;
        z-index: 1000;
        transition: left .3s;
        
        &.show {
            left: 0;
            transition: left .3s;
        }
    }

    nav {
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        
        ul {
            width: 100%;
            height: 80%;
            list-style: none;
            display: flex;
            flex-direction: column;
            justify-content: space-between;

            li {
                text-align: center;

                a {
                    display: block;
                    text-decoration: none;
                    font-weight: 500;
                    font-size: 17px;
                    padding: 20px;
                    transition: background-color .3s;

                    &:hover{
                        background-color: #8d8bee;
                    }
                }

                &:last-of-type {
                    margin: 0;
                }
            }
        }    
    }   
    
    @media screen and (min-width: 481px) {
        header {
            height: auto;
            left: 0;
            padding: 0 20px;
            background-color: #7f7df3c4;
            display: flex;
            justify-content: end;
            // TODO: cambiar a space-between
        }

        nav {
            width: max-content;
            height: auto;
            justify-content: end;

            ul {
                width: max-content;
                height: auto;
                flex-direction: row;
                justify-content: flex-start;

                li {
                    a {
                        padding: 12px;
                        font-size: 16px;
                    }
                }
            }
        }
    }
</style>

<script>
    const $btnIcon = document.querySelector('#btnWrapper div')
    const $header = document.querySelector('header')
    const $links = document.querySelectorAll('nav ul li')
    const $nav = document.querySelector('nav')

    const openCloseNavbar = () => {
        $btnIcon?.classList.toggle('close')
        $header?.classList.toggle('show')
    }

    if (window.innerWidth < 481) {
        $btnIcon?.addEventListener('click', () => {
            openCloseNavbar()
        })

        $nav?.addEventListener('click', () => {
            openCloseNavbar()
        })
    }
</script>