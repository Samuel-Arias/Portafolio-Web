<section id="inicio">
    <div class="template">Aqui va mi cara</div>

    <div class="saludo">
        <p>Hola soy<br></b><strong>Samuel Arias</strong></p>
        <p>y soy <strong>Frontend Developer</strong></p>
    </div>

    <img src="../../public/assets/svg/waveTop.svg" alt="Wave">
    <img src="../../public/assets/svg/waveMiddleTop.svg" alt="Wave">
    <img src="../../public/assets/svg/waveMiddleBottom.svg" alt="Wave">
    <img src="../../public/assets/svg/waveBottom.svg" alt="Wave">
</section>

<style lang="scss">
    #inicio {
        width: 100%;
        height: 100vh;
        position: relative;
        display: flex;
        flex-direction: column;
        align-items: center;
        color: #FFF;
    }

    .template {
        width: 100%;
        height: 190px;
        margin-top: 20px;
    }

    .saludo {
        width: max-content;
        margin-top: 24px;
        text-align: center;
        font-weight: 300;

        p:first-of-type {
            font-size: 35px;
            line-height: 39px;

            strong {
                font-weight: 500;
            }

            margin-bottom: 12px;
        }

        p:last-of-type {
            font-size: 18px;

            strong {
                font-weight: 300;
                position: relative;
                margin-left: 3px;
                display: inline-block;

                &::before {
                    content: '';
                    background-color: #1A188F;
                    position: absolute;
                    top: 0;
                    left: -104%;
                    width: 103%;
                    height: 103%;
                    z-index: -1;
                }

                &::after {
                    content: '';
                    background-color: #CD3D3D;
                    position: absolute;
                    top: 0;
                    left: 50%;
                    transform: translateX(-151%);
                    width: 103%;
                    height: 103%;
                    z-index: -2;

                    @keyframes grow {
                        to {
                            transform: translateX(-50%);
                        }
                    }

                    animation: grow 1s ease-in-out forwards 1.5s;
                }
            }
        }
    }
    
    img {
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        bottom: -92px;
        animation: 1s forwards ease-out;
        z-index: -1;

        &:last-of-type {
            @keyframes showFirst {
                from {
                    bottom: -92px;
                }
                to {
                    bottom: -1px;
                }
            }
            animation-name: showFirst;
        }
        &:nth-of-type(3) {
            @keyframes showSecond {
                from {
                    bottom: -92px;
                }
                to {
                    bottom: 60px;
                }
            }
            animation-name: showSecond;
        }
        &:nth-of-type(2) {
            @keyframes showThird {
                from {
                    bottom: -92px;
                }
                to {
                    bottom: 122px;
                }
            }
            animation-name: showThird;
        }
        &:first-of-type {
            @keyframes showFourth {
                from {
                    bottom: -92px;
                }
                to {
                    bottom: 183px;
                }
            }
            animation-name: showFourth;
        }
    }
</style>